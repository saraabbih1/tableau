#include<stdio.h>
int main(){
    int n,i,j;
    printf("donner le nombre n");
    scanf("%d",&n);


    int tab[n];
    printf("donner les elements du tableau:");
    for(i=0;i<=n;i++){
        scanf("%d",&tab[n]);
    }

    int invtab[n];
    for(i=0;i<=n;i++){
       sacanf(" %d ",&invtab[n]) ;
    }
    for(i=)
    invtab[n]=
    return 0;
     }
    